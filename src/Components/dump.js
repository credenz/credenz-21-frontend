/* eslint-disable no-lone-blocks */
{
  /* <div className="col-12">
  <div
    className={
      "1" === active
        ? `focused`
        : Math.abs(1 - parseInt(active)) === 1
        ? `not-focused`
        : "not-displayed"
    }
    id="1"
    ref={card1}
    onClick={() => {
      // setActive("1");
    }}
  >
    <EventCard2
      icon={RC}
      width={width}
      height={height}
      title="Reverse Coding"
      text="Hone your problem-solving skills by decrypting complex questions"
    />
  </div>
  <div
    className={
      "2" === active
        ? `focused`
        : Math.abs(2 - parseInt(active)) === 1
        ? `not-focused`
        : "not-displayed"
    }
    id="2"
    ref={card2}
    onClick={() => {
      // setActive("2");
    }}
  >
    <EventCard2
      icon={Clash}
      width={width}
      height={height}
      title="Clash"
      text="Clash is based on Competitive Programming!"
    />
  </div>
  <div
    className={
      "3" === active
        ? `focused`
        : Math.abs(3 - parseInt(active)) === 1
        ? `not-focused`
        : "not-displayed"
    }
    id="3"
    ref={card3}
    onClick={() => {
      // setActive("3");
    }}
  >
    <EventCard2
      icon={NTH}
      width={width}
      height={height}
      title="NTH"
      text="Decrypt the clues to solve complex puzzles and race your way to finish at top!"
    />
  </div>
  <div
    className={
      "4" === active
        ? `focused`
        : Math.abs(4 - parseInt(active)) === 1
        ? `not-focused`
        : "not-displayed"
    }
    id="4"
    ref={card4}
    onClick={() => {
      // setActive("4");
    }}
  >
    <EventCard2
      icon={Wallstreet}
      width={width}
      height={height}
      title="WallStreet"
      text="Get an insight on stock markets by Investing and trading virtual money!"
    />
  </div>
  <div
    className={
      "5" === active
        ? `focused`
        : Math.abs(5 - parseInt(active)) === 1
        ? `not-focused`
        : "not-displayed"
    }
    id="5"
    ref={card5}
    onClick={() => {
      // setActive("5");
    }}
  >
    <EventCard2
      icon={BPlan}
      width={width}
      height={height}
      title="B-Plan"
      text="Showcase your entrepreneurial skills by presenting innovative business plans to the world!"
    />
  </div>
</div>; */
}
